import { _decorator } from "cc";
const { ccclass, property } = _decorator;

import { MapObjectData } from "../model/object";

@ccclass("MonsterData")
export class MonsterData extends MapObjectData {
}
